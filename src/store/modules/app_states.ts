const state = {
    page: "/aaaas"
};
const getters = {
    currentPage:(state)=>state.page
};
const actions =  {};
const mutations = {};

export default{
    state,
    getters,
    actions,
    mutations
}